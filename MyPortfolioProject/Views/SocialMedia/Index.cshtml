@model List<SocialMedia>

@{
	ViewData["Title"] = "Index";
	Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<div class="content" style="margin-bottom:20em;">
	<div class="container-fluid">
		<h4 class="page-title">Sosyal Medya</h4>
		<div class="row">
			<div class="col-md-12">
				<div class="card">
					<div class="card-header">
						<div class="card-title">Sosyal Medya Listesi</div>
					</div>
					<div class="card-body">
						<div class="card-sub">
							Sosyal Medya linkleri ile ilgili işlemleri aşağıdaki panelden yapabilirsiniz.
						</div>
						<table class="table mt-3">
							<thead>
								<tr>
									<th scope="col">Başlık</th>
									<th scope="col">Url</th>
									<th scope="col">Sosyal Medya Icon</th>
									<th scope="col">Sil</th>
									<th scope="col">Güncelle</th>
								</tr>
							</thead>
							<tbody>
								@foreach (var item in Model)
								{
									<tr>
										<td>@item.Title</td>
										<td><a href="@item.Url" target="_blank">@item.Url</a></td>
										<td>@item.Icon</td>
										<td><a data-id="@item.Id" asp-controller="SocialMedia" asp-action="Update" asp-route-socialMediaId="@item.Id" class="btn btn-success">Güncelle</a></td>
										<td><a data-id="@item.Id" asp-controller="SocialMedia" asp-action="Delete" asp-route-socialMediaId="@item.Id" class="btn btn-danger">Sil</a></td>
									</tr>
								}
							</tbody>
						</table>
						<a asp-controller="SocialMedia" asp-action="Create" class="btn btn-outline-primary">Yeni Sosyal Medya Link Girişi</a>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>


