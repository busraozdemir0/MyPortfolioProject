@model List<SocialMedia>

<ul class="home-social">
    @foreach (var item in Model)
    {
        <li>
            <a href="@item.Url" target="_blank"><i class="@item.Icon" aria-hidden="true"></i><span>@item.Title</span></a>
        </li>
    }
</ul>